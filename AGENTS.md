# Halfday project instructions

Read and follow `CLAUDE.md` for Shopify architecture, CLI workflow, brand, performance, and audit scope. It is the shared project guidance for coding agents.

<!-- @bufferapp/cli skill — managed -->
!buffer context
<!-- /@bufferapp/cli skill — managed -->

The Buffer CLI was unavailable during initial setup. Buffer is unrelated to theme synchronization; do not install or configure it as part of Shopify work unless needed for an authorized task.
